using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DontDistroyOnLoad : MonoBehaviour
{
    private static DontDistroyOnLoad s_Instance = null;
    void Start()
    {
        if (s_Instance == null) { 
        }
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
